@extends('layouts.public') @section('title','Confirmar identidad')
@section('content')<main class="ambient-page"><div class="public-shell"><section class="card identity-card"><div class="identity-avatar"><span class="material-symbols-outlined">person</span></div><span class="eyebrow">Estás accediendo como</span><h1>{{ $juror->name }}</h1><p>{{ $juror->title }}</p><p class="form-hint">{{ $event->name }}</p><div class="secure-note"><span class="material-symbols-outlined">verified_user</span><span>Confirma que este acceso te corresponde antes de continuar.</span></div><div class="identity-actions"><form action="{{ route('jury.confirm') }}" method="post">@csrf<input type="hidden" name="confirmation_token" value="{{ $confirmationToken }}"><button class="button button-block" type="submit">Sí, continuar</button></form><a class="button button-secondary button-block" href="{{ route('jury.access') }}">No soy esta persona</a></div></section></div></main>@endsection
