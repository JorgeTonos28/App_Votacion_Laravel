@extends('layouts.admin') @section('title','Configuración')
@section('content')<header class="page-heading"><div><h1>Configuración</h1><p>Seguridad, usuarios y plantillas globales de la plataforma.</p></div><a class="button" href="{{ route('admin.mfa') }}"><span class="material-symbols-outlined">shield_lock</span> Segundo factor</a></header><section class="metric-grid"><article class="card metric-card"><span class="metric-label">Administradores</span><div class="metric-value">{{ $metrics['administrators'] }}</div></article><article class="card metric-card"><span class="metric-label">Operadores</span><div class="metric-value">{{ $metrics['operators'] }}</div></article><article class="card metric-card"><span class="metric-label">Cuentas con MFA</span><div class="metric-value">{{ $metrics['mfa'] }}</div></article></section><div class="content-grid"><section class="card panel"><header class="panel-header"><h2>Plantillas activas</h2></header><div class="panel-body report-grid">@foreach($templates as $template)<article class="card report-card"><div><strong>{{ $template->name }}</strong><div class="table-subtitle">{{ $template->description }}</div></div><span class="material-symbols-outlined">deployed_code</span></article>@endforeach</div></section><aside class="card panel"><header class="panel-header"><h2>Ajustes técnicos</h2></header><div class="panel-body stack">@if($settings->isEmpty())<p class="form-hint">No hay ajustes globales sobrescritos.</p>@endif @foreach($settings as $setting)<div><strong>{{ $setting->key }}</strong><div class="table-subtitle">{{ $setting->value }}</div></div>@endforeach</div></aside></div>@endsection
