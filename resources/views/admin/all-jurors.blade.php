@extends('layouts.admin') @section('title','Jurados')
@section('content')<header class="page-heading"><div><h1>Jurados</h1><p>Directorio transversal de jurados autorizados por evento.</p></div></header><section class="card panel"><header class="panel-header"><h2>Directorio de Jurados</h2><span class="form-hint">{{ $jurors->count() }} registros</span></header><div class="table-wrap"><table class="data-table"><thead><tr><th>Jurado</th><th>Evento</th><th>Estado</th><th>Último acceso</th><th>Evaluaciones</th><th></th></tr></thead><tbody>@foreach($jurors as $juror)<tr><td><strong>{{ $juror->name }}</strong><div class="table-subtitle">{{ $juror->title }}</div></td><td>{{ $juror->event->name }} <code>{{ $juror->event->code }}</code></td><td><span class="badge {{ $juror->status==='Active'?'badge-success':'badge-muted' }}">{{ $juror->status }}</span></td><td>{{ $juror->last_access_at?->setTimezone('America/Santo_Domingo')->format('d/m/Y H:i')??'Sin acceso' }}</td><td>{{ $juror->votes_count }}</td><td><a class="icon-button" href="{{ route('admin.jurors',$juror->event_id) }}"><span class="material-symbols-outlined">open_in_new</span></a></td></tr>@endforeach</tbody></table></div></section>@endsection
